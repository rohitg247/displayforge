# ── Frontend (baked into JS bundle at build time) ──────────────────────────────
# Set to your server's public IP or domain before running: npm run build
VITE_API_URL=http://YOUR_SERVER_IP:8000

# ── Backend ────────────────────────────────────────────────────────────────────
# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=replace-with-a-random-64-char-hex-string

# Comma-separated list of allowed frontend origins (no trailing slash)
CORS_ORIGINS=http://YOUR_SERVER_IP:3200
